$(info The name of the shared library to be created is: $(SHLIB))


#PKG_LIBS = @RCPPPAR_LIB@

OBJECTS.zigzag = \
    RcppHarmonic.o \
    RcppZigZag.o \
    RcppExports.o

OBJECTS = $(OBJECTS.zigzag)
